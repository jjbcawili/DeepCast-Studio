{
  "name": "deepcast-background-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply deepcast-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply deepcast-db --remote"
  },
  "dependencies": {
    "jose": "^6.0.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260801.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.28.1"
  }
}
