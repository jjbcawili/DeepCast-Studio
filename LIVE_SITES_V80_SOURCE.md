# DeepCast Studio — deployed Sites v80 source

The exact deployable text source is preserved in `sites-v80/`. Production binary assets are recorded by SHA-256 in `sites-v80/ASSET_MANIFEST.json` and preserved in the verified Drive source archive. The existing main implementation remains untouched.
